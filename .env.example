# ics-website 后端配置
# cp .env.example .env 然后填入真实值

# MySQL（连接主项目的 MariaDB 容器）
DB_HOST=mysql
DB_PORT=3306
DB_R_USER=readonly_user
DB_R_PASSWORD=your_readonly_password
DB_META=course_scheduler_meta

# 服务端口
SERVER_PORT=8000
